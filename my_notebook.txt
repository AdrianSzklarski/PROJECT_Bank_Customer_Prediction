1. Przewiduję kategorię karty, którą klient wybierze lub bank może próbować sprzedać komuś i tą kategorię;
'Card Category'

2. Commands to work in the terminal
pip install --upgrade pip
pip freeze > requirements.txt
pip install -r requirements.txt
